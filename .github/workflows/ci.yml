name: catalog-ci

on:
  pull_request:
    paths:
      - "providers/**"
      - "models/**"
      - "scripts/**"
  push:
    branches: [main]
    paths:
      - "providers/**"
      - "models/**"
      - "scripts/**"

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install pyyaml
      - name: Validate changed catalog entries
        run: |
          set -e
          files=$(git diff --name-only origin/main...HEAD -- 'providers/*.yaml' 'models/*.yaml' || true)
          if [ -z "$files" ]; then
            echo "No catalog files changed."
            exit 0
          fi
          echo "$files"
          python3 scripts/catalog_validate.py $files

  secret-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: gitleaks
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
